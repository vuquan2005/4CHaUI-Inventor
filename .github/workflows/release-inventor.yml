name: Release Inventor

on:
    push:
        branches:
            - main
        paths-ignore:
            - ".github/**"
            - ".gitignore"
            - ".gitattributes"
            - "README.md"
    workflow_dispatch:

jobs:
    release-projects:
        runs-on: ubuntu-latest
        permissions:
            contents: write

        steps:
            - name: Checkout Repository
              uses: actions/checkout@v5
              with:
                  fetch-depth: 2

            - name: Zip and Release Changed Projects
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              run: |
                  TIMESTAMP=$(date -u '+%Y-%m-%d %H:%M:%S UTC')
                  FULL_SHA="${{ github.sha }}"
                  SHORT_SHA="${FULL_SHA:0:7}"
                  EVENT_NAME="${{ github.event_name }}"

                  if [[ "$EVENT_NAME" == "workflow_dispatch" ]]; then
                    DIFF_BASE=""
                    echo "🚀 Manual trigger - Archiving all existing directories."
                  elif git rev-parse HEAD^ >/dev/null 2>&1; then
                    DIFF_BASE="HEAD^"
                    echo "🔍 Comparing changes with previous commit ($DIFF_BASE)"
                  else
                    DIFF_BASE=""
                    echo "⚠️ First commit - Will archive all existing directories."
                  fi

                  for DIR in */; do
                    DIR="${DIR%/}"

                    if [[ "$DIR" == .* ]]; then continue; fi

                    if [[ -n "$DIFF_BASE" ]]; then
                      if git diff --quiet "$DIFF_BASE" HEAD -- "$DIR"; then
                        echo "⏩ Skipping [$DIR]: No changes."
                        continue
                      fi
                    fi

                    echo "🛠️ Processing project: $DIR"

                    zip -r "${DIR}.zip" "$DIR" \
                      -x "*/OldVersions/*" \
                      -x "*.bak" \
                      -x "*.lck" \
                      -x "*.tmp" \
                      -x "*/.git*"

                    echo "♻️ Updating Rolling Tag for: $DIR"
                    gh release delete "$DIR" --cleanup-tag -y || true
                    
                    sleep 2

                    RELEASE_NOTES="### 📝 Update Information
                    - **Project:** \`$DIR\`
                    - **Last modified in commit:** [\`$SHORT_SHA\`](${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }})
                    - **Time:** $TIMESTAMP
                    - **Triggered by:** @${{ github.actor }}"

                    gh release create "$DIR" "${DIR}.zip" \
                        --title "$DIR" \
                        --notes "$RELEASE_NOTES"
                      
                    echo "✅ Successfully updated project: $DIR"
                  done
